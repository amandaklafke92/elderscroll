# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor / IDE
.vscode/
.idea/
*.swp

# Logs / scratch
*.log

# Large local-only media (share via Drive/Dropbox, not git).
# Exception: tutorial videos are tracked so teammates get the canonical
# set when they pull, without a separate sync step.
*.mp4
*.MP4
!Videos/*.mp4
!Videos/*.MP4
!assets/tutorials/*.mp4
!assets/tutorials/*.MP4

# Local scratch notes (not part of the product)
backlog.md
substack-draft_elderscroll.md
substack/

# Local Claude Code state
.claude/
